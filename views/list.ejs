<html>
<head>
  <meta charset="UTF-8">
  <title>Bookings</title>
  <link rel="stylesheet" type="text/css" href="style.css" >

  
 </head>
    <body>
    <div class="title">
        <H1>Welcome, <%= user.name %></H1>
        </div>
		<hr/>
		<p>You logged in through <b><%= user.type %></b>, user ID is <%= user.id %></p>
        <H2>Bookings (<%= nBookings %>)</H2>
        <ul>
            <% for (var b of bookings) { %>
            <li>Booking name: <a href="/details?_id=<%= b._id %>"><%= b.bookingname %></a></li>
            <% } %>
        </ul>
        <a class="button" href='/create'>Create a New Booking</a>
		<p><a class="button"href='/logout'>Logout</a></p>
    </body>
</html>
